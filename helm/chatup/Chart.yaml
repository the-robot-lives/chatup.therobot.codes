apiVersion: v2
name: chatup
description: "chatup.therobot.codes static landing page"
version: 0.1.0
appVersion: "0.1.0"
type: application
# static-site subchart is vendored directly under charts/static-site (not
# fetched via `helm dependency update`) — the file://../../../../components
# path escaped the repo and ArgoCD couldn't resolve it. No `dependencies:`
# block needed; Helm auto-discovers unpacked subcharts under charts/.
